{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="max-w-6xl mx-auto p-6">
    <h1 class="text-3xl font-semibold mb-6">Witaj, {{ user.email }}!</h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {% set cards = [
            { title: "🎣 Dodaj połów", description: "Zarejestruj nową rybę." },
        ] %}

        {% for card in cards %}
            <a href="test" class="block p-6 bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-2xl shadow hover:shadow-md transition">
                <div class="text-xl font-medium mb-2">{{ card.title }}</div>
                <p class="text-zinc-500 dark:text-zinc-400">{{ card.description }}</p>
            </a>
        {% endfor %}
    </div>
</div>
{% endblock %}
